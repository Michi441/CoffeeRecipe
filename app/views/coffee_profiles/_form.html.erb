<%= form_with(model: coffee_profile, local: true) do |form| %>
  <% if coffee_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coffee_profile.errors.count, "error") %> prohibited this coffee_profile from being saved:</h2>

      <ul>
      <% coffee_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
